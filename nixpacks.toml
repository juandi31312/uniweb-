[phases.build]
cmds = ["pip install -r requirements.txt", "python manage.py collectstatic --noinput"]

[phases.deploy]
cmds = ["python manage.py migrate"]

[start]
cmd = "gunicorn uniweb.wsgi --log-file -"
