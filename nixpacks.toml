[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn uniweb.wsgi --bind 0.0.0.0:$PORT --log-file -"
